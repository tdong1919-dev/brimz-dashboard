{
  "name": "brimz-e2e",
  "private": true,
  "version": "0.1.0",
  "description": "Playwright end-to-end tests for the Brimz dashboard",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed --workers=1",
    "test:watch": "SLOWMO=400 playwright test --headed --workers=1",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
